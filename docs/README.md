# Documentation

## Working documents

These two are current. Everything else describes how something is built; these say what is being
worked on and what is wrong with it.

- **[Roadmap](roadmap.md)** — the slicing plan: what is done, what comes next
- **[Bug List](bug_list.md)** — every known defect, numbered, ranked by severity

## Chapters

- **[Orientation](orientation/README.md)** — what the venue is, how to build it, how it is tested
- **[The framework](framework/README.md)** — `pubsub_itc_fw`: the reactor, threading, allocators, pinning
- **[The venue](venue/README.md)** — the deployable components, one document each
- **[Pub/sub](pubsub/README.md)** — topic publish and subscribe, the MEP, and OAR
- **[Durability](durability/README.md)** — the write-ahead log and replay
- **[High availability](availability/README.md)** — surviving a process death, and a machine death
- **[FIX](fix/README.md)** — the protocol, the codec, and the session layer
- **[Operations](operations/README.md)** — metrics, secure communications, load profiles
- **[History](history/README.md)** — the session log and release notes
- **[Superseded](superseded/README.md)** — working notes kept for provenance only

Every script lives in `scripts/` and is run from the repository root:
`python3 scripts/build.py`, `./scripts/devsetup.sh`, `python3 scripts/ha_test.py --scenario all`.
See [Building](orientation/building.md).

## How this is arranged

A chapter is a directory, a section is a file in it, and a subsection is a heading inside that
file. Reading order lives in this page, never in the filenames — a number in a filename is a
position, and positions change, while 115 references from the source cite these documents by path.

`python3 scripts/check_docs.py` enforces it: every link resolves, every document is reachable from
this page, every documentation path named in the source exists, and every box in the clickable
[architecture map](orientation/architecture.md) still points at a live anchor.

## API reference

Generated by Doxygen. Run `./scripts/build.sh --doxygen` then open `build/doxygen/html/index.html`.
